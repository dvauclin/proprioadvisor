// Deprecated: WebSite JSON-LD is now injected globally via layout using websiteJsonLd()
// File kept for backward-compat imports but returns null
import React from 'react';

const WebsiteStructuredData: React.FC = () => null;

export default WebsiteStructuredData; 

